# Sources

- Local probing found After Effects installed but no Photoshop-style
  `AfterFX.Application` COM automation ProgID.
- Adobe CEP host panels expose `window.__adobe_cep__.evalScript(...)` for
  running ExtendScript in the host app.
